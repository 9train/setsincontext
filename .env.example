# Optional JSON session persistence for demos and small pilots.
# Leave unset to keep sessions, participants, and invites memory-only.
# SESSION_STORE_FILE=.runtime-data/sessions.json

# Optional room map persistence path used by the runtime mapper flow.
# MAP_FILE=./data/room_maps.json
